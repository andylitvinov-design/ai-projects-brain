# /audit-sale markers compatibility alias

The singular `/audit-sale` scorecard is deprecated.

Use the single canonical scorecard:

- `systems/audit-sales-markers.md`

Do not maintain separate marker weights or scoring rules for `/audit-sale`.
