# Psihotavr source identity blocker

Verified 2026-08-13.

The intended Psihotavr canonical source is not currently resolvable through the connected production systems:

- GitHub lookup for `andylitvinov-design/psihotavr` returns not found through the connected GitHub installation.
- The current Vercel team inventory has no project named `psihotavr`.
- The previously verified production deployment `dpl_HAJrrmZomBEqZkfee4deorefMeF9` is no longer found through the connected Vercel account.
- The separate healthy Vercel project `psitherapy` maps to `andylitvinov-design/report`, branch `main`. It is already a distinct `business_growth_outcomes` denominator item and must not be substituted for Psihotavr or counted twice.

Until Owner Verification establishes whether Psihotavr should be restored, reconnected, or formally retired/replaced, treat Psihotavr as `SOURCE_IDENTITY_NEEDS_OWNER_VERIFICATION`, not as an implementation-ready product target. An automated retirement/replacement would change portfolio semantics and is not allowed.
