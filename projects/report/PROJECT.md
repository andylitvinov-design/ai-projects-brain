# report

## 1. Purpose

Design reference and implementation brief for a dynamic HTML/React A4 report page template, with target project **Алхимия Души**.

## 2. Agent Entry

Use this project when the user mentions:

- report template;
- dynamic report page;
- A4 report;
- Алхимия Души report design;
- report-page-2 reference.

Read order:

1. `PROJECT.md`
2. `CHECKS.md` if present
3. repo-local `README.md`
4. `docs/design-references/reports/report-page-2-brief.md`
5. `docs/design-references/reports/codex-prompt.md`

## 3. Live URLs

- production: not applicable / needs verification
- preview: needs verification
- admin: not applicable / needs verification

## 4. Repositories

- canonical repo: https://github.com/andylitvinov-design/report
- repo visibility: public
- repo status: active, not archived
- default branch: `main`
- code search index: not indexed according to GitHub inventory
- related repo: `andylitvinov-design/dao-usin-bach-report-kit` exists and may be related by topic; relationship needs verification

## 5. Hosting / Deploy

- provider: not confirmed
- project name: needs verification
- deploy source: needs verification
- branch: `main` for repo source
- deploy rules: needs verification

## 6. Current Status

- Public repo exists and is active.
- Root README confirms this is a design reference / implementation brief repo, not necessarily a deployed app.
- Root `package.json` not found, so there may be no root app/build commands.

## 7. Important Files

Confirmed from repo README:

- `docs/design-references/reports/report-page-2-brief.md`
- `docs/design-references/reports/sample-report-data.json`
- `docs/design-references/reports/report-page-2-reference-small.base64.txt`
- `docs/design-references/reports/restore-reference-image.mjs`
- `docs/design-references/reports/codex-prompt.md`

## 8. Environment Variable Names

Only names are listed. Values must never be stored here.

- none known / needs verification

## 9. Known Issues

- This appears to be a reference/brief repo, not a production app.
- No root `package.json` found during verification.
- Live URL and deploy source are not confirmed.

## 10. Recent Tasks

- Design reference and implementation brief for dynamic A4 report page are stored in repo docs.

## 11. Next Actions

1. Inspect design brief before implementation tasks.
2. Restore visual reference with `node docs/design-references/reports/restore-reference-image.mjs` if needed.
3. Verify whether this repo is only a reference repo or also has a deployment target.
4. Verify relationship with `dao-usin-bach-report-kit` before cross-repo assumptions.

## 12. Risks

- Treating reference repo as production app.
- Inventing root build commands.
- Confusing report reference with `dao-usin-bach-report-kit`.
- Losing visual fidelity to the reference image.

## 13. Rules for Codex

- Mark uncertain assumptions as `needs verification`.
- Do not invent deployment/build commands.
- Use README-listed docs as source of truth for report template work.
- Do not store private client data in report examples.

## 14. Verification Status

- repo mapping: verified from GitHub metadata and README
- repo status: public/active/not archived verified from metadata
- purpose: verified from README
- important docs: verified from README
- root package/build: not found
- live/deploy mapping: needs verification
- relationship to `dao-usin-bach-report-kit`: needs verification
