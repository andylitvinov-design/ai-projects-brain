# PR Delivery Sweep — 2026-07-25

## Result

- Terminal state: `MERGED_WAITING_DEPLOY`.
- Target metric: `product_delivery_rate`.
- Before score: `33.3` (`1/3` selected product/operational chains reached `LIVE_VERIFIED`).
- Selected chain: Finance PayPal hold-import normalization and expense-channel filtering.
- Project: `andylitvinov-design/finance`.

## Real delivery change

- Reused existing PR #188 instead of creating a duplicate.
- Rechecked current `main` and confirmed the intended changes were still absent.
- PR scope remained one commit and four files:
  - `api/paypal-transactions.js`;
  - `expense-ledger-feed.js`;
  - `tests/paypal-transactions-api.test.mjs`;
  - `tests/expense-ledger-feed.test.cjs`.
- Proven behavior added:
  - request balance-affecting PayPal records;
  - reject T1501/T1503 authorization-hold rows, non-success states and explicit preauthorization/temporary-hold text;
  - add an expense-channel filter;
  - restrict visible totals and category saves to the selected channel.
- Existing branch was closed/reopened once to request current PR-event checks.
- Available PR-triggered workflows completed successfully, but they were Binance-specific and do not replace the targeted regression suite.
- PR was cleanly mergeable against current `main` and squash-merged with expected head SHA.
- PR: https://github.com/andylitvinov-design/finance/pull/188
- Head SHA: `36bf76dea922d0e6b0a59ad4293e5a5933de1bef`.
- Merge SHA: `aeecdc4ba585a921d355a94534d1a63d837ef847`.
- Canonical `main` now contains `PAYPAL_NON_LEDGER_EVENT_CODES` and the narrow UI/filter changes.

## Metric inputs

| Input | Before | Current proven state | Expected after live verification |
| --- | ---: | ---: | ---: |
| Product chains `LIVE_VERIFIED` | 1 | 1 | 2 |
| Selected product/operational chains | 3 | 3 | 3 |
| Product Delivery Rate | 33.3 | 33.3 | 66.7 |
| Live Completion Rate | 33.3 | 33.3 | 66.7 if this chain requires and passes live proof |

No score increase is claimed from merge evidence alone.

## Verification boundary

- Repository reachability: proven; merge SHA is canonical.
- Production deploy/source mapping: not proven through the available provider path in this run.
- Production PayPal behavior and expense-channel UI: not yet independently verified.
- Current terminal state therefore remains `MERGED_WAITING_DEPLOY`.

## Next automatic action

1. Wait for or locate the Finance production deployment sourced from merge `aeecdc4ba585a921d355a94534d1a63d837ef847`.
2. Verify `/api/status` maps production to that merge or a descendant on `main`.
3. Run a read-only PayPal test fixture/live-safe request proving T1501/T1503 holds do not appear as expenses.
4. Verify desktop/mobile `Учет расходов → Расходы` channel selection, visible row totals and category-save scoping with no console/network error.
5. Only then transition to `LIVE_VERIFIED` and update Product Delivery Rate numerator from `1` to `2`.

## Remaining blockers

- Provider/deployment observability was unavailable in the current tool path; this is not classified as owner action.
- Saved historical hold rows are not mutated by this change. Any cleanup requires a separate dry-run and evidence-backed data repair.
