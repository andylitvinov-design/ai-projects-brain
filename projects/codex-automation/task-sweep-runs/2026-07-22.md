# Morning Task Sweep — 2026-07-22

## Source coverage

- Re-read `systems/live-upgrade-delivery-contract.md`, `systems/remote-pr-auto-creation.md`, the durable Task Sweep memory, the 2026-07-21 Evening closure, and the 2026-07-22 PR Delivery Sweep.
- Inspected current GitHub issues, PRs, commits, branch/PR pairings and available commit statuses for the selected active chains.
- Rechecked Vercel production deployment and direct production responses for Toronto Tantra.
- Rechecked Netlify current production deploy for Brain Management.
- No synced local/computer Codex filesystem or unified ChatGPT-triggered task index was available. No claim is made about unsynced worktrees, local-only branches or private task history.

## LIVE_VERIFIED

No new chain reached `LIVE_VERIFIED` in this discovery run.

## Reconciled unresolved chains

### 1. Toronto Tantra facilitator portrait — `MERGED_WAITING_DEPLOY`

- Canonical `torontotantra/main` is merge `df432e020f13271bb5e7697bcb4a336f3abda322` from PR #39.
- Vercel production deploy `dpl_CPEQWRUmu54L4V8UUCXAvX3BTymG` is READY and maps exactly to that merge SHA.
- The production HTML returns 200 and references `/images/andrey-litvinov-facilitator-v6.jpg` in the facilitator block.
- The direct production JPEG returns 200, `image/jpeg`, and `Content-Length: 15006`, matching the verified asset size accepted by PR #39.
- Failed stage: clean-session desktop/mobile full-frame browser decode plus console/network verification. Transport and byte-length proof do not prove target-device rendering.
- Next automatic action: perform clean desktop/mobile visual verification of the production page and confirm no broken icon, alt-text fallback, partial gray decode, failed image request or console error. Do not create another image PR unless the defect reproduces.

### 2. Brain Management dashboard publication — `BLOCKED_BY_OWNER`

- Current `brain-management/main` is `41891b1193619262bdbf7ae069b6125cd83c8989` and contains the strategic renderer and single canonical publisher.
- The canonical workflow supports merged-PR, `main` push and manual dispatch, checks out current `main` after merge, and targets the existing Netlify site.
- Current commit status exposes no Actions result or failed run ID that can be safely rerun through the connected GitHub tool.
- Netlify still reports production deploy `6a59bc16f349e3e190a47208` as current and READY; it is the old 2026-07-17 deploy and predates the strategic renderer/current snapshot.
- Failed stage: owner start of the existing canonical publisher, followed by matching Netlify deploy, snapshot/receipt binding and visible Portfolio verification.
- Exact owner action: run `Publish System Health Dashboard` once on `main`: https://github.com/andylitvinov-design/brain-management/actions/workflows/publish-system-health-dashboard.yml
- Next automatic action after owner start: inspect the run; rerun only failed jobs once if needed; verify deploy source SHA, public snapshot, receipt, `project-health.mjs`, desktop/mobile Portfolio values and console/network state.

### 3. Codex Links command-create authorization — `NO_SAFE_UPGRADE`

- Issue #174 remains open and accurately records the high-severity missing authorization boundary.
- No open implementation PR exists.
- Searches for current remote branches containing `auth` or `command` found no valid fresh-main implementation branch.
- Stale PR #163 remains source material only and was not revived.
- Failed stage: no safe implementation artifact exists. A one-line browser token guard would break the primary UI and is not an acceptable repair.
- Next automatic action: create or reuse one minimal fresh-main owner-session or short-lived capability branch when it appears; then automatically open the PR and require anonymous, valid, expired/replayed, double-submit and public-serialization regressions.

### Owner-gated lower-priority chains

- Ezohata PR #41 — `BLOCKED_BY_OWNER`: draft remains open, mergeable and broad at 172 changed files. Production Supabase Google provider activation plus one genuine owner login and one non-owner denial are still required before merge.
- PsiTherapy PR #122 — `BLOCKED_BY_OWNER`: draft remains open and requires Firebase project/provider/domain/environment access plus authenticated preview and production proof.
- Finance stale backlog — `NO_SAFE_UPGRADE`: no new current-main safe PR was identified in the inspected window; stale branches were not rebased or merged wholesale.

## Duplicate and PR reconciliation

- Toronto Tantra PRs #38 and #39 form one canonical portrait chain; no duplicate PR is needed.
- Brain Management has no remaining equivalent branch/PR to reuse; direct-main commits are reachable and not wrong-base losses.
- Codex Links issue #174 has no current implementation branch without a PR.
- Every selected existing remote delivery branch had an accessible PR. No remote branch without an accessible PR was proven.
- `LOST_MERGED_WRONG_BASE`: not detected.
- Merged commit not reachable from canonical production branch: not detected.
- `REGRESSION_DEFAULT_STATE_NOT_VERIFIED`: remains resolved for Finance PR #623 and was not reopened.

## Upgrade Candidate Score and Morning handoff

Formula: `impact * urgency * completion_probability / risk`, each factor on the canonical 1–5 rubric. Carryover wins ties.

| Rank | Chain | Impact | Urgency | Completion probability | Risk | Priority | Morning action |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | Toronto Tantra production portrait verification | 4 | 5 | 4 | 1 | 80 | Close the existing product regression through clean desktop/mobile decode and console/network proof; no new image PR unless reproduced. |
| 2 | Brain Management publisher/deploy/live closure | 5 | 5 | 2 | 1 | 50 | After the exact owner workflow start, verify run -> deploy -> snapshot -> receipt -> visible Portfolio. Do not create another trigger repair. |
| 3 | Codex Links issue #174 authorization artifact | 5 | 5 | 3 | 3 | 25 | Proceed only from a valid fresh-main implementation branch; auto-create/reuse its PR and require the full auth regression matrix. |

The queue contains two product/operational chains and one infrastructure carryover. The infrastructure exception is justified by a proven portfolio observability regression but remains owner-blocked.

## Evidence gaps

- No synced computer/local Codex source was accessible.
- No unified ChatGPT-triggered task-history index was accessible.
- GitHub commit status exposes no Brain Management publisher run or failed job ID for the current `main`.
- Toronto Tantra has exact source/deploy/HTTP/asset-size proof, but no target-browser screenshot or console/network capture.

## Persistence boundary

This record preserves discovery, reconciliation and ranked handoff only. It does not claim production success from merge, READY deploy, HTTP 200, MIME type or byte length alone.
