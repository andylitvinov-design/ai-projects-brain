# Books Data Schema

Current source-backed content and generated-data schemas are inside the Books repository. The public production branch contains Markdown remedy content, JavaScript/TypeScript data modules, source inventories and media assets. Private prescription records introduce a separate stateful schema whose storage resource, persistence path and migration/retention policy still need verification before the capability can be called production-ready.
