# Weekly AI System Learnings

> Append one compact evidence-backed section per week. Do not copy full task reports.

## Template

### Week ending YYYY-MM-DD

- **Key failures:** what materially failed or remained unfinished.
- **Root causes:** proven causes; uncertain items marked `needs verification`.
- **Repeated patterns:** issues seen more than once.
- **Fixes implemented:** links/files/PRs and verification.
- **Still open:** owner, blocker, and next exact action.
- **Efficiency changes:** context, tool, verification and automation signals.
- **Rules/index updates:** reusable lessons added to durable memory.

## Recorded weeks

### Week ending 2026-07-26 — Brain Management control-plane architecture

- **Key failures:** Brain Management production publication drifted from repository schema; canonical 24-metric history covered only `3/7` required dates; metric-level `source_refs` were absent; the action queue lacked the full measurable-input contract; one 100→100 collector refresh appeared among completed improvements; Weekly Delivery System Review had no structured operational receipt.
- **Root causes:** direct Vercel deployment bundles and repository source diverged; immutable history was introduced after the week started; evidence remained attached mainly to chains/global records; weekly review persistence was not enforced; regression tests retained obsolete routes and assumptions.
- **Repeated patterns:** merge/source readiness was mistaken for publication completeness; bundles omitted newly added routes; old tests verified previous UI/data contracts rather than current behavior.
- **Fixes implemented:** Brain Management PR #80 added read-only history/control-plane audits and current regression tests; PR #81 expanded the direct source bundle to include Trends, health/history APIs and immutable-snapshot dependencies.
- **Still open at cutoff:** attach evidence references to all metrics, accumulate four future honest snapshots, enrich action items with measurable inputs/effects, classify 100→100 events as collector refreshes, persist weekly-review output and publish exact-source health/history routes.
- **Efficiency changes:** weekly review separated source validation, regression repair and production verification without changing formulas.
- **Rules/index updates:** numeric 24/24 coverage alone is insufficient. Health also requires formula versions, raw inputs, sources, immutable history, unique ownership, anti-gaming guards, memory sync, complete action contracts and structured weekly persistence.

### Week ending 2026-08-01 — Production closure, API contracts and durable sync

- **Key failures:** the machine portfolio registry still routed Brain Management to historical Netlify after the capsule and live product had moved to Vercel; two canonical API routes returned the HTML application shell with HTTP 200; product delivery/task success/live completion remained a shared `1/4`; provider/live readiness remained `0/4`; the first API-repair design would have exceeded the Vercel Hobby limit with 14 functions.
- **Root causes:** durable registry reconciliation lagged behind operational recovery; prior checks accepted status without validating response body/content type; the route repair initially added functions instead of reusing the constrained shared runtime; the delivery-conversion handoff did not identify one exact denominator item and canonical implementation repository.
- **Repeated patterns:** operational fixes can reach production before durable indexes are reconciled; a successful transport status can hide the wrong application payload; direct/publication paths fail when dependency or platform-budget closure is incomplete; low metrics can generate duplicate work unless correlated inputs are explicitly deduplicated.
- **Fixes implemented:** Brain Management PR #161 moved canonical API readiness `3/5 → 5/5`; production verified six routes, four Overview aggregates, 24 metrics, ten Trends, seven agents, six manifest shortcuts and no runtime-error cluster. Daily publication then produced fresh public business-KPI evidence `4/6` without formula changes. Weekly refresh reconciled the canonical Vercel mapping.
- **Still open:** owner-session provider proof; exact shared `1/4` denominator/deliverable; Psihotavr source state; complete seven-day/per-metric evidence.
- **Efficiency changes:** reconciliation removed duplicate/terminal chains; PR hygiene reduced verified open PRs; function-budget diagnosis replaced an over-budget implementation with a compliant shared-function solution.
- **Rules/index updates:** API success requires status, content type, body and parseability. Platform-budget closure is part of deployability. Correlated metrics with one raw input must share one implementation chain.

### Week ending 2026-08-02 — Delivery execution quality

- **Key failures:** the canonical rolling delivery denominator remained `1/4`; one of three completion claims required correction; one of three completed chains required rework; weekly scorecard publication topology was incomplete; recovery/publication guards overlapped routine publication and closure language.
- **Root causes:** shared `1/4` lacked denominator identities and selected product; P0 release regressions consumed implementation capacity; implementation did not always consume the exact ranked assignment; rolling weekly fields lacked live per-day identities.
- **Repeated patterns:** infrastructure recovery displaced product outcomes; technically valid blocker removal drifted from assigned chain; detection guards risked becoming duplicate executors; repository source presence was mistaken for published endpoint availability.
- **Fixes implemented:** production recovered with frozen six-tab contract, 24 metrics, ten projects/trends and canonical JSON APIs; assignment-source parity, exact-manifest parity and atomic UI/data guards were strengthened; Weekly Delivery findings were persisted in PR #190 and Sunday Dashboard findings in PR #189.
- **Still open:** provider owner blocker; exact delivery denominator; Psihotavr classification; dependency-closed weekly review publication.
- **Efficiency changes:** rolling evidence remained weak on product completion and rework; P0 recovery justified temporarily infrastructure-heavy work but not its continuation.
- **Rules/index updates:** implementation must consume exact ranked `chain_id` or return it for reconciliation; detection/recovery guards require a single-owner lock; rolling weekly fields are not additive events without immutable identities.

### Week ending 2026-08-08 — Guard maturity rose while operational liveness regressed

- **Key failures:** Brain Management current operational data aged past the 18-hour ceiling and all five canonical operational APIs now fail closed with `503 STALE_OPERATIONAL_DATA_FORBIDDEN`; no fresh complete attributable current-main artifact was available in the active recovery chain; repeated hosted GitHub workflows failed before checkout with `steps=null` and unavailable logs; current scheduler evidence has no active recurring primary implementation-stage owner and no active recurring atomic publication-stage owner; Psihotavr's historical GitHub mapping now returns 404 and is absent from the 30-repository owner enumeration.
- **Root causes:** release safety/guard coverage improved faster than routine liveness ownership; the current recovery chain depends on a complete attributable artifact while hosted CI repeatedly fails before repository execution; durable automation docs lagged scheduler reality; historical project identity was previously preserved without a fresh repo-reachability check. The exact reason the Psihotavr repo is inaccessible is `needs verification` — deletion, rename, transfer or permission change must not be inferred.
- **Repeated patterns:** an older `LIVE_VERIFIED` state was treated as if it were durable current health until later freshness evidence superseded it; infrastructure recovery continued to consume attention; provider/repository identities proliferated or drifted faster than durable routing; pre-checkout runner failures can be mistaken for branch-code failures and cause duplicate recovery work.
- **Fixes implemented:** Brain Management added atomic operational source refresh, reduced-payload rejection and production source-parity fail-closed guards during Aug 5–6; rule lifecycle moved `3 active/9 → 6 active/9` by Aug 4 without formula change; the stale-source guard correctly blocks current publication rather than masking it. AI Projects Brain PR #191 recorded the repeated pre-step CI reachability failure class and the exact-head/no-duplicate-recovery rule. PR #192 upgraded `/audit-ui` to v1.5 with accessible form-completion/authentication checks. This weekly refresh enumerated all 30 accessible owner repositories and reconciled root human/machine indexes to a compact current format.
- **Still open:** Brain Regression Guard retains the single `production-source-parity-recovery-20260806` chain until one complete attributable artifact reaches route-complete preview and identical canonical production; scheduler governance must restore explicit recurring implementation and atomic publication owners; Morning readiness must publish the exact shared `1/4` denominator and one product deliverable; Owner Verification must complete the EzoHata Finance read-only provider proof; Psihotavr canonical source/provider identity must be re-proven before code or KPI credit; temporary Vercel probe/recovery projects need a later bounded cleanup/retention review, not deletion by this refresh.
- **Efficiency changes:** guard correctness and false-success prevention improved, but operational availability/freshness worsened. Repository discoverability improved from incomplete inventory to 30 known owner repositories. Identity routing improved by downgrading Psihotavr instead of sending work to a 404 repo. Product-delivery conversion has no new verified gain in this refresh.
- **Rules/index updates:** `LIVE_VERIFIED` is time-bound and must be downgraded by later stale/source-parity evidence; current scheduler evidence overrides historical automation documentation; a required role absent from the scheduler is `UNASSIGNED_IN_SCHEDULER`, not silently inherited by a guard; repository existence and active-product identity are separate inventories; a historical repo that is currently unreachable becomes `NEEDS_VERIFICATION`; diagnostic provider projects never become canonical identities by accident. `projects.md`, `projects.json`, `data/project-index.json`, `projects/index.md`, portfolio registry, Brain Management capsule and Psihotavr capsule were reconciled accordingly.

### Week ending 2026-08-15 — Enabled execution, broken assignment context, no direct metric gain

- **Key failures:** canonical Brain Management freshness regressed to source age 102.9h; four core APIs fail closed while `/api/needs-attention` still serves the same stale source with HTTP 200; `/sw.js` is 404; source parity remains open. Morning System Upgrade is enabled but received no consumable assignment. The latest complete Weekly Delivery scorecard still has 0/7 current immutable scored days and 0 product/business closures.
- **Root causes:** Aug 15 proved `CAPABILITY_CONTEXT_OMITTED`: Daily Strategic Priorities treated GitHub Actions billing/spending as the exhaustive publication path and omitted the already verified connected direct Vercel production channel. Therefore `publication_freshness` was falsely rejected as owner-blocked. The longer-running publication defect also combines missing attributable dependency-closed artifact parity with no runnable routine atomic-publisher assignment.
- **Repeated patterns:** guard and receipt quality improve faster than operational liveness; enabled roles are described as capacity even when their inputs are incomplete; recovery work consumes the implementation window while the shared product denominator remains `1/4`; behavior recovery is repeatedly confused with source-parity closure.
- **Fixes implemented:** Sunday Dashboard Review PR #313 corrected the false terminal source-parity state and anchored seven-day history to the current date. The Aug 15 Morning System Upgrade repaired the broken handoff and documented the connected Vercel channel, but correctly made no deployment and claimed no metric gain. This refresh records one machine-readable durable root-cause candidate for the next Daily Strategic Priorities scan.
- **Still open:** Brain Regression Guard retains `production-source-parity-recovery-20260806` until explicit reassignment or terminal proof; Daily Strategic Priorities must re-rank with complete capability context; atomic publication must reach source <=18h, 5/5 correct fresh APIs, `sw.js` 200 and parity true; immutable snapshots must resume prospectively; one product/business deliverable must follow the P0; Finance remains owner-blocked and Psihotavr identity remains unresolved.
- **Efficiency changes:** open PR inventory grew from 34 at the Aug 9 scorecard to 36 on Aug 15; current-source age worsened from ~48h on Aug 8 to 102.9h; 0 verified metric gains were produced by the Aug 15 handoff/repair cycle. Catalog discoverability stayed stable at 30 repositories / 10 active identities.
- **Rules/index updates:** an enabled executor without a complete assignment is `PIPELINE_BROKEN`; capability discovery must be passed into ranking; a disproven blocker requires re-rank before the implementation window ends; mixed stale-success and fail-closed API behavior fails the atomic publication contract; docs/index work is `NO_DIRECT_METRIC_EFFECT`.

### Week ending 2026-08-22 — Evidence continuity improved; zero-effect completion regressed

- **Key failures:** the Aug 10–16 scorecard had only 1/7 immutable days, 0/1 fully valid LIVE_VERIFIED chains, two false-success corrections, 100% rework on the one selected chain and zero product/business upgrades. During Aug 17–22 freshness repeatedly crossed the 18-hour terminal ceiling before reactive recovery. After PR #396 prohibited zero-effect `DONE`, canonical `/api/trends` again published five no-effect pilots as `DONE`.
- **Root causes:** build/source and closure authority overlapped during Aug 10–16; prospective history publication still lacks a proven enabled scheduler; later Trends refresh/release cycles did not preserve the effect-postprocessor invariant, so corrected semantic state was overwritten by source regeneration.
- **Repeated patterns:** guards improve in code but are bypassed or overwritten in the final published dataset; operational repair consumes the implementation slot; live behavior is healthier than durable/operational registry truth; reports and evaluated pilots are mistaken for completed delivery.
- **Fixes implemented:** Sunday Dashboard Review PR #354 added 24/24 formula/source evidence, four anti-gaming guards, complete queue fields and one honest Aug 16 snapshot. Weekly Delivery PR #357 corrected source-parity/terminal claims. PR #396 added the zero-effect contract. PRs #398–#400 restored an Aug 22 coherent five-API source and corrected embedded freshness fallback. Immediate canonical re-read confirms 5/5 HTTP 200, parity true and `/sw.js` 200.
- **Still open:** Evening Delivery Closure must perform the delayed same-source checkpoint; the Trends effect postprocessor must become release-invariant; Daily Dashboard Update/Brain Regression Guard/Brain Data Freshness Watch must be removed from operational capacity claims or restored as exactly one enabled non-overlapping scheduler; immutable history needs six more prospective days; one named product denominator item must reach real metric effect; Finance and Psihotavr remain blocked.
- **Efficiency changes:** immediate publication health recovered, but product delivery stayed `1/4`; five evaluated Trends pilots produced no metric gain; history moved only 0/7 → 1/7; scheduler reconciliation exposed three stale assignment names. All durable edits are `NO_DIRECT_METRIC_EFFECT`.
- **Rules/index updates:** zero-effect tasks are `EVALUATED_NO_EFFECT`, never `DONE`; semantic postprocessors must run after every collector refresh and before release; operational assignments must map to enabled scheduler evidence; immediate recovery and delayed terminal closure are distinct; `memory_sync_status` must reference current durable evidence.

### Week ending 2026-08-29 — Semantic truth improved; continuity and causal binding remain broken

- **Key failures:** the latest complete scorecard (Aug 17–23) has only `1/7` immutable days, 9 implementation/effect chains, 2 infrastructure LIVE receipts, 0 numeric metric gains, 0 product/business outcomes, 3 delayed freshness regressions, 17 same-chain repair PRs and 13 noncanonical terminal receipts. Canonical weekly-review API still serves Aug 10–16. Current Aug 23–29 history is again `1/7`. Persistent reached live but left `live_completion_rate` at `1/4`; the next generic capability task is again bound to that metric without a proven eligible denominator item.
- **Root causes:** no enabled exclusive routine publisher owns current data/history/review publication; an implementation-worker schedule was repeatedly mistaken for publication cadence; metric assignment checks field completeness but not whether the task can enter the immutable metric numerator; release-lock state is not closed atomically when the queue marks a task terminal.
- **Repeated patterns:** technically valid pilots and control-plane repairs consume implementation capacity while product metrics remain unchanged; stronger guards coexist with stale published review/history; stages outside PR Delivery still merge most operational PRs; enabled-but-exhausted temporary automations remain visible as apparent capacity.
- **Confirmed improvements:** current Trends queue now truthfully exposes one `EVALUATED_NO_EFFECT` and nine READY tasks instead of zero-effect `DONE`; required operational APIs, 24 metrics, ten projects, six-route PWA and service worker are readable; current 24h runtime error inventory is zero; `/copy-ui` merged as a durable cross-project workflow without becoming a new catalog identity.
- **Still open:** prove causal denominator eligibility before task 3766 is consumed; restore one routine publisher and two attributable cycles; clear terminal task 3763's release lock; publish the Aug 17–23 weekly review; accumulate six missing prospective snapshots; reconcile memory sync; complete Finance owner proof and resolve Psihotavr identity.
- **Efficiency changes:** semantic truthfulness improved, but effect conversion stayed at zero. Latest complete week spent 17 PRs on one freshness chain and produced no numeric or product/business gain. Catalog discoverability remained stable at 30 repositories / 10 identities.
- **Rules/index updates:** a metric assignment needs `denominator_item_id` plus causal eligibility; enabled finite schedules with no remaining occurrence are `ENABLED_BUT_EXHAUSTED`; release locks close or explicitly rebind at terminal queue transition; current weekly review publication is part of dependency closure. All refresh edits are `NO_DIRECT_METRIC_EFFECT`.
