# Weekly AI System Learnings

> Append one compact evidence-backed section per week. Do not copy full task reports.

## Template

### Week ending YYYY-MM-DD

- **Key failures:** what materially failed or remained unfinished.
- **Root causes:** proven causes; uncertain items marked `needs verification`.
- **Repeated patterns:** issues seen more than once.
- **Fixes implemented:** links/files/PRs and verification.
- **Still open:** owner, blocker, and next exact action.
- **Efficiency changes:** context, tool, verification and automation signals.
- **Rules/index updates:** reusable lessons added to durable memory.

## Recorded weeks

### Week ending 2026-07-26 — Brain Management control-plane architecture

- **Key failures:** Brain Management production publication drifted from repository schema; canonical 24-metric history covered only `3/7` required dates; metric-level `source_refs` were absent; the action queue lacked the full measurable-input contract; one 100→100 collector refresh appeared among completed improvements; Weekly Delivery System Review had no structured operational receipt.
- **Root causes:** direct Vercel deployment bundles and repository source diverged; immutable history was introduced after the week started; evidence remained attached mainly to chains/global records; weekly review persistence was not enforced; regression tests retained obsolete routes and assumptions.
- **Repeated patterns:** merge/source readiness was mistaken for publication completeness; bundles omitted newly added routes; old tests verified previous UI/data contracts rather than current behavior.
- **Fixes implemented:** Brain Management PR #80 added read-only history/control-plane audits and current regression tests; PR #81 expanded the direct source bundle to include Trends, health/history APIs and immutable-snapshot dependencies.
- **Still open at cutoff:** attach evidence references to all metrics, accumulate four future honest snapshots, enrich action items with measurable inputs/effects, classify 100→100 events as collector refreshes, persist weekly-review output and publish exact-source health/history routes.
- **Efficiency changes:** weekly review separated source validation, regression repair and production verification without changing formulas.
- **Rules/index updates:** numeric 24/24 coverage alone is insufficient. Health also requires formula versions, raw inputs, sources, immutable history, unique ownership, anti-gaming guards, memory sync, complete action contracts and structured weekly persistence.

### Week ending 2026-08-01 — Production closure, API contracts and durable sync

- **Key failures:** the machine portfolio registry still routed Brain Management to historical Netlify after the capsule and live product had moved to Vercel; two canonical API routes returned the HTML application shell with HTTP 200; product delivery/task success/live completion remained a shared `1/4`; provider/live readiness remained `0/4`; the first API-repair design would have exceeded the Vercel Hobby limit with 14 functions.
- **Root causes:** durable registry reconciliation lagged behind operational recovery; prior checks accepted status without validating response body/content type; the route repair initially added functions instead of reusing the constrained shared runtime; the delivery-conversion handoff did not identify one exact denominator item and canonical implementation repository.
- **Repeated patterns:** operational fixes can reach production before durable indexes are reconciled; a successful transport status can hide the wrong application payload; direct/publication paths fail when dependency or platform-budget closure is incomplete; low metrics can generate duplicate work unless correlated inputs are explicitly deduplicated.
- **Fixes implemented:** Brain Management PR #161, merge `92cec78d0ee474ef53db904ec97abe5521a5824f`, reused existing functions and moved canonical API readiness `3/5 → 5/5`; production verified six routes, four Overview aggregates, 24 metrics, ten Trends, seven agents, six manifest shortcuts and no runtime-error cluster. Daily Dashboard Update then published fresh evidence at commit `1e297dfb9b0dc7bc4a327319107b27db47c706a8`, including public business-KPI coverage `4/6`. This weekly refresh reconciled the canonical Vercel mapping and closed the stale registry discrepancy in a focused documentation PR.
- **Still open:** `Owner Verification` must complete one current EzoHata Finance session/read-only provider journey; Daily Strategic Priorities must select one concrete deliverable for the shared `1/4` input; Psihotavr collector/live/source state remains `needs verification`; legacy `projects.md`, `projects.json` and `data/project-index.json` need bounded field reconciliation; complete seven-day history and per-metric source coverage must be accumulated honestly.
- **Efficiency changes:** Morning reconciliation removed four duplicate/terminal chains; PR hygiene reduced verified open PRs `46 → 36`, merged three focused docs PRs and closed seven obsolete/superseded PRs; implementation-owner conflicts remained zero; one diagnostic iteration replaced the over-budget 14-function candidate with a compliant shared-function solution.
- **Rules/index updates:** API success requires status, content type, body and parseability. Platform-budget closure is part of deployability. Correlated metrics with one raw input must share one implementation chain. Operational recovery and durable catalog sync are separate stages, and Weekly Brain Refresh owns the latter.

### Week ending 2026-08-02 — Delivery execution quality

- **Key failures:** the canonical rolling delivery denominator remains `1/4` for product delivery, task success and live completion; one of three completion claims required correction; one of three completed chains required rework; the current weekly scorecard endpoint is absent from production even though an older source file remains in the repository; the enabled recovery/publication guards overlap the exclusive publication and closure stages.
- **Root causes:** the shared `1/4` chain still lacks the exact four denominator items and one selected deliverable/repository; direct-deploy and atomic-release regressions consumed most verified implementation capacity; Daily Strategic Priorities selected the bounded Psihotavr reachability chain, while the next Morning System Upgrade implemented assignment-source parity and publication guards instead of consuming that assignment; weekly denominators are exposed as rolling metrics without a live per-day history endpoint.
- **Repeated patterns:** infrastructure recovery displaces product outcomes; valid technical blocker removal can drift from the assigned chain; detection guards become duplicate executors when emergency recovery boundaries are not explicit; a repository source file is not proof that an operational endpoint is published.
- **Fixes implemented:** canonical production is again `LIVE_VERIFIED` with the frozen six-tab UI, four Overview aggregates, 24 metrics, ten projects, ten Trends, seven agent cards and five JSON operational APIs; assignment-source parity moved `0/1 → 1/1` without claiming metric points; exact-manifest parity and atomic UI/data release guards were strengthened; the weekly review was persisted as an immutable Brain Management scorecard on a focused branch.
- **Still open:** `Owner Verification` retains the provider `0/4` blocker; Morning Task Sweep must publish the exact four delivery denominator items and one existing product deliverable; Daily Strategic Priorities retains the Psihotavr deployment/domain/collector classification; weekly review publication must use the dependency-closed canonical topology rather than an unguarded extra function.
- **Efficiency changes:** current rolling evidence is live completion `1/4`, rework `1/3`, false-success correction `1/3`, production repair `1/3`, autonomous recovery `1/1`, validation `26/27`, latest duplicate cleanup `4`, active carryover `3`, and one owner blocker. Observed verified closure mix was one operational/API outcome to two infrastructure/control-plane outcomes, below the `3:1` product guardrail but justified by repeated P0 release regressions.
- **Rules/index updates:** implementation must consume the exact ranked `chain_id` or explicitly return it for reconciliation before substituting another repair. Detection/recovery guards require a single-owner lock and must not become routine parallel publishers. Rolling weekly fields must not be treated as additive events without immutable daily identities.

### Week ending 2026-08-30 — Delivery execution quality

- **Key failures:** the prior publisher-cadence recovery candidate failed; delayed freshness regressions increased from 3 to 9, the last observed publisher interval was 23.5 hours against a 12-hour requirement, and the current canonical release manifest returns 404. Eight Trend pilots reached live behavior but all eight produced zero assigned-metric effect. Brain Management created 57 PRs and merged 52 while PR Delivery Sweep merged or repaired 0.
- **Root causes:** Daily Dashboard Update is the nominal exclusive publisher, but Brain Regression Guard and implementation/recovery stages still refresh, deploy or self-close the same operational chain; implementation bypasses the exclusive GitHub delivery stage; receipt writers put diagnostic labels such as `LIVE_VERIFIED_NO_EFFECT_EXPLAINED`, `DEPLOYMENT_PENDING` and `PIPELINE_BROKEN` into top-level `terminal_state`; the active-chain, priority and data surfaces are published independently.
- **Repeated patterns:** one-shot recovery is mistaken for cadence; infrastructure/tool pilots displace measured product work; live behavior is conflated with same-metric improvement; merged activity bypasses independent PR delivery and closure; immutable history remains partial at 1/7.
- **Fixes implemented:** Brain Management PR #476 proposes one immutable weekly scorecard and one continuation recovery candidate on the existing freshness chain. It records exact denominators, zero effect, current live regression and ownership defects without changing formulas, runtime or production. The report and PR receive zero improvement credit.
- **Still open:** the existing freshness chain requires two publisher-owned cycles at <=12 hours, manifest/source/deployment attribution, zero recovery PRs and delayed independent closure; Finance remains `BLOCKED_BY_OWNER` at 0/4; the current exact Trend assignment and `/api/data` active-chain snapshot disagree; one of six due Evening Closure receipts is missing.
- **Efficiency changes:** exact weekly denominator is 12 implementation/effect chains: eight Trend pilots, one recurring freshness recovery, one Actions-quota cleanup, one Management Action Report chain and one Sunday dashboard/Overview chain. Canonical `LIVE_VERIFIED` count is 0/12; live-behavior zero-effect pilots are 8/8; verified numeric gains are 0; product/business gains are 0.
- **Rules/index updates:** detection may hand off but must not publish under the exclusive publisher lease; diagnostic outcome labels must map to one of the four canonical terminal states; PR/CI/merge must pass through PR Delivery Sweep; active-chain omission requires an explicit terminal/exclusion record; a schedule, merge, deploy or documentation PR is never cadence or metric effect proof.

### Current partial week through 2026-09-05 — Causality rule not enforced; activity accelerated without effect

- **Key failures:** thirteen Aug 31–Sep 5 Trends pilots reached live behavior and every assigned metric remained unchanged; all thirteen receipts still used noncanonical `LIVE_VERIFIED_NO_EFFECT_EXPLAINED`. The Sep 5 source again aged to 23.0h and four APIs failed closed before reactive refresh. Immutable history remains 1/7, the weekly API remains on Aug 17–23, the complete release gate is red, and open PR inventory grew from 42 to 49 with zero ready.
- **Root causes:** the Aug 29 causal-binding candidate was documented but not made an executable ranking gate. Assignments still pair generic synthetic pilots with immutable ledger metrics even when the pilot cannot add or reclassify a denominator item. Routine publication still has no enabled Daily Dashboard Update scheduler; Regression Guard-labelled work is emitted under another scheduler and creates dated refresh chains. The full release gate spans source, fixture, dependency and harness owners and cannot be closed by one bounded patch.
- **Repeated patterns:** field-complete baseline/target metadata is mistaken for causal eligibility; implementation, merge, deployment and self-terminal receipt happen in one stage; semantic correction exists in the queue but not the top-level receipt enum; one-shot fresh recovery is mistaken for cadence; daily operational handoffs are produced while daily immutable scored snapshots are absent.
- **Confirmed improvements:** the Sep 5 recovery restored 7/7 APIs, `/sw.js` and a 299-file release manifest from a 1.3h source; seven-day runtime-error query is zero; queue rotation preserves 7 `EVALUATED_NO_EFFECT` plus 3 READY rather than false `DONE`; the failed multi-owner gate attempt was closed instead of partially merged. Books was added to durable routing with confirmed repo evidence and explicit pre-production uncertainty.
- **Still open:** Daily Strategic Priorities must enforce the continued causal candidate; scheduler ownership must resolve Daily Dashboard Update versus Regression Guard-labelled execution; six prospective history dates and Aug 24–30 weekly publication are missing; the complete release gate needs owner-partitioned fixes; Books needs one canonical branch/Vercel project plus hosted checks and provenance/safety review; Finance and Psihotavr remain blocked.
- **Efficiency changes:** thirteen implementation/deployment cycles generated zero assigned-metric points; five due independent closures yielded zero canonical LIVE closure; Sep 3–5 agent reports show 3 recovery reports, 5 regressions detected/recovered, 0 live upgrades and 0 metric points; PR backlog grew by seven.
- **Rules/index updates:** a metric assignment must identify a pre-existing immutable denominator item and prove the exact transition the implementation can cause; a scheduler-less actor label is attribution drift, not capacity; current history requires append-only daily snapshots and cannot be reconstructed from routine receipts; meaningful pre-production projects belong in the extended catalog without entering the production overlay; duplicate provider projects are aliases until one canonical target is proven. All durable edits are `NO_DIRECT_METRIC_EFFECT`.
