# Agent Memory Candidates

This file stores possible lessons found by `/learn-pass`.

Candidates are not active rules yet.

Promote only when:

- the user confirms;
- the pattern repeats;
- the lesson prevents a high-risk workflow failure;
- `/memory-review` decides evidence is strong enough.

Do not load this file by default except during `/learn-pass` or `/memory-review`.
